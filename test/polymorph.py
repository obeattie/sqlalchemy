# test basic polymorphic relationship with a single tasble

# test polymorphic relationship with multiple tables

# test that loading an object A from its non-polymorphic mapper returns the same identity as from its polymorphic mapper